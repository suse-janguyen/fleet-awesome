---
defaultNamespace: cert-manager

labels:
  fleet-bundle: jetstack.cert-manager

helm:
  chart: cert-manager
  repo: https://charts.jetstack.io
  version: 1.16.5
  releaseName: cert-manager
  takeOwnership: false
  values:
    crds:
      enabled: true

###

---
defaultNamespace: default

labels:
  fleet-bundle: caddy-server

helm:
  chart: oci://zot.kubefred.com/caddy/caddy
  version: 0.1.3
  releaseName: caddy-server
  takeOwnership: false
#  valuesFrom:
#    - configMapKeyRef:
#        name: caddy-server-values
#        key: values.yaml
#    - secretKeyRef:
#        name: caddy-server-values
#        key: values.yaml


###
